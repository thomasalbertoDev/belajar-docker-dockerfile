# Fungsi ini digunakan untuk melakukan build image dari Dockerfile yang ada di folder 1.from
docker build -t xriot45/from 1.from

# Fungsi ini digunakan untuk menampilkan semua image yang ada
docker image ls